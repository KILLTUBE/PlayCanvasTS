/**
 * @namespace
 * @name pc.string
 * @description Extended String API
 */
namespace pc.string {
    export function format(...args: any[]) {
        console.log(arguments);
        return "";
    }
}
